name: skillswap

services:
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    platform: linux/amd64
    restart: unless-stopped
    env_file: ./server/.env
    ports:
      - 5000:5000
      

